<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.zippy.shop.mapper.ShopMapper">
	<!-- 전체조회 -->
	<select id="getProductList" resultType="ProductVO">
		SELECT * FROM
		shop_product
	</select>
	<!-- 카테고리 조회 -->
	<select id="getCategoryList" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.*, b.comp_name FROM shop_product s
		JOIN business
		b
		ON s.email = b.email
		WHERE s.category =
		#{category}
	</select>
	<!-- 키워드 조회 -->
	<select id="getKeywordList" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.* , b.comp_name FROM shop_product s
		JOIN
		business b
		ON s.email = b.email
		WHERE
		s.pro_name LIKE
		'%'||#{keyword}||'%'
		OR s.category LIKE
		'%'||#{keyword}||'%'
		OR
		s.email IN
		(SELECT email FROM
		business
		WHERE comp_name
		LIKE '%'||#{keyword}||'%'
		AND member_type=2)
	</select>
	<!-- 단건조회(디테일) -->
	<select id="getProduct" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.*, b.comp_name FROM shop_product s
		JOIN business
		b
		ON s.email = b.email
		WHERE s.pro_no =
		#{proNo}
	</select>
	<!-- 상세이미지 불러오기 -->
	<select id="getDetailImg" resultType="String"
		parameterType="ProductVO">
		SELECT pro_img
		FROM shop_product_img
		WHERE
		pro_no = #{proNo}
	</select>
	<!-- 상세옵션 불러오기 -->
	<select id="getDetailOpt" resultType="ProductOptionVO"
		parameterType="ProductOptionVO">
		SELECT *
		FROM shop_product_option
		WHERE pro_no = #{proNo}
	</select>
	
	<!-- 장바구니 -->
	<!-- 장바구니 등록 --> 
	<insert id="insertCart" parameterType="CartVO">
		INSERT INTO shop_cart
		VALUES(
		( SELECT NVL( MAX(cart_no) , 0) + 1 FROM shop_cart),
		#{email},
		#{cartPno},
		#{cartOptNo},
		#{cartQty})
	</insert>
	<!-- 장바구니 리스트 조회 -->
	<select id="getCartList" resultType="ProductVO"
		parameterType="CartVO">
		SELECT s.*, b.comp_name
		FROM shop_product s
		JOIN shop_cart c
		ON c.cart_pno = s.pro_no
		JOIN business b
		ON s.email = b.email
		WHERE s.pro_no IN
		(SELECT cart_pno
		FROM shop_cart
		WHERE email = #{email})
	</select>
	<!-- 장바구니 개수 조회 -->
	<select id="getMyCart" resultType="Integer"
		parameterType="CartVO">
		SELECT count(*) countMycart
		FROM shop_cart
		WHERE email = #{email}
	</select>

</mapper>