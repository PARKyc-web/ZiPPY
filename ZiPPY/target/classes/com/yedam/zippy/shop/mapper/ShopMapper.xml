<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.zippy.shop.mapper.ShopMapper">
	<!-- 전체조회 -->
	<select id="getProductList" resultType="ProductVO">
		SELECT * FROM
		shop_product
	</select>
	<!-- 카테고리 조회 -->
	<select id="getCategoryList" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.*, b.comp_name FROM shop_product s
		JOIN business
		b
		ON s.email = b.email
		WHERE s.shop_category =
		#{shopCategory}
	</select>
	<!-- 키워드 조회 -->
	<select id="getKeywordList" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.* , b.comp_name FROM shop_product s
		JOIN
		business b
		ON s.email = b.email
		WHERE
		s.shop_product_name LIKE
		'%'||#{keyword}||'%'
		OR s.shop_category LIKE
		'%'||#{keyword}||'%'
		OR
		s.email IN
		(SELECT email FROM
		business
		WHERE comp_name
		LIKE '%'||#{keyword}||'%'
		AND member_type=2)
	</select>
	<!-- 단건조회(디테일) -->
	<select id="getProduct" resultType="ProductVO"
		parameterType="ProductVO">
		SELECT s.*, b.comp_name FROM shop_product s
		JOIN business
		b
		ON s.email = b.email
		WHERE s.shop_product_no =
		#{shopProductNo}
	</select>
	<!-- 상세이미지 불러오기 -->
	<select id="getDetailImg" resultType="String"
		parameterType="ProductVO">
		SELECT shop_product_img
		FROM shop_product_img
		WHERE
		shop_product_no = #{shopProductNo}
	</select>
	<!-- 상세옵션 불러오기 -->
	<select id="getDetailOpt" resultType="ProductOptionVO"
		parameterType="ProductOptionVO">
		select *
		from shop_product_option
		where shop_product_no = #{shopProductNo}
	</select>
	<!-- 장바구니 -->
		
	<!-- 장바구니 등록 --> 
	<insert id="insertBasket" parameterType="BasketVO">
		INSERT INTO shop_basket
		VALUES(
		( SELECT NVL( MAX(basket_no) , 0) + 1 FROM shop_basket),
		#{email},
		#{basketProductNo},
		#{basketProductOptNo},
		#{basketProductQty})
	</insert>

</mapper>